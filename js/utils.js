function toast(m, tp = 'info') { const c = document.getElementById('toastC'), cs = { info: 'border-chain-info bg-chain-info/10 text-chain-info', success: 'border-chain-accent bg-chain-accent/10 text-chain-accent', warn: 'border-chain-warn bg-chain-warn/10 text-chain-warn', danger: 'border-chain-danger bg-chain-danger/10 text-chain-danger' }; const t = document.createElement('div'); t.className = `toast px-3 py-2 rounded-lg border text-[11px] font-medium ${cs[tp] || cs.info}`; t.textContent = m; c.appendChild(t); setTimeout(() => t.remove(), 4000); }
function gD(n, mn, mx) { const d = []; let v = mn + Math.random() * (mx - mn); for (let i = 0; i < n; i++) { v += (Math.random() - .48) * (mx - mn) * .08; v = Math.max(mn, Math.min(mx, v)); d.push(v); } return d; }
function gL(n) { const l = [], nw = new Date(); for (let i = n - 1; i >= 0; i--) { const d = new Date(nw); d.setDate(d.getDate() - i); l.push((d.getMonth() + 1) + '/' + d.getDate()); } return l; }

function iC(k, mn, mx, c = 10) { const d = []; let v = mn + Math.random() * (mx - mn); for (let i = 0; i < c; i++) { const o = v; v += (Math.random() - .47) * (mx - mn) * .06; v = Math.max(mn, Math.min(mx, v)); d.push({ o, c: v, h: Math.max(o, v) + Math.random() * (mx - mn) * .02, l: Math.min(o, v) - Math.random() * (mx - mn) * .02 }); } CD[k] = d; }
iC('btc', 98000, 106000); iC('eth', 2400, 2600); iC('tvl', 170, 190); iC('gas', 8, 40);

function rC(id, k) { const el = document.getElementById(id); if (!el) return; const d = CD[k]; let aH = -Infinity, aL = Infinity; d.forEach(c => { if (c.h > aH) aH = c.h; if (c.l < aL) aL = c.l; }); const r = Math.max(aH - aL, 1); el.innerHTML = d.map(c => { const u = c.c >= c.o, bT = ((aH - Math.max(c.o, c.c)) / r) * 28, bH = Math.max(((Math.max(c.o, c.c) - Math.min(c.o, c.c)) / r) * 28, 1); return `<div class="c ${u ? 'up' : 'dn'}"><div class="w" style="height:${Math.max(28 - bH - bT, 1)}px;margin-bottom:${bT}px"></div><div class="b" style="height:${bH}px"></div></div>`; }).join(''); }